export interface OpenAIModel {
    id: string;
    name: string;
  }

export interface AnthropicModel {
    id: string;
    name: string;
  }

export interface MistralModel {
    id: string;
    name: string;
  }